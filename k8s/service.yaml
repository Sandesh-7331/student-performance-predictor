apiVersion: v1
kind: Service

metadata:
  name: student-performance-service

spec:

  selector:
    app: student-performance

  type: NodePort

  ports:

    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007
